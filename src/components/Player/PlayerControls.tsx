import React from 'react';
import {
  View,
  StyleSheet,
  Dimensions,
  TouchableHighlight,
} from 'react-native';
import { Icons } from '@themes/Styleguide';
import { scaleSize } from '@utils/scaleSize';

type TPlayerControlsProps = {};

const PlayerControls: React.FC<TPlayerControlsProps> = () => {
  return (
    <View></View>
  );
};

const styles = StyleSheet.create({
});

export default PlayerControls;
